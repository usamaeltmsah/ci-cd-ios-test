# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ci-cd-ios-test' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ci-cd-ios-test

  target 'ci-cd-ios-testTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ci-cd-ios-testUITests' do
    # Pods for testing
  end

end
